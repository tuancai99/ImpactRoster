import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    submit: {
      main: "#18ba46",
      dark: "#129337",
      contrastText: "#fff",
    },
  },
});

export default theme;
